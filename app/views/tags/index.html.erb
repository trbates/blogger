<h1>All Tags</h1>
<ul id="tags">
	<% @tags.each do |tag| %>
	<li>
		<%= link_to tag.name, tag_path(tag) %>
		<% if logged_in? %>
			<span> - <%= link_to "edit", edit_tag_path(tag) %></span>
		<% end %>
	</li>
	<% end %>
</ul>